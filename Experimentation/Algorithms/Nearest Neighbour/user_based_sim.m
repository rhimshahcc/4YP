
function user_based_sim(row_pos,col_pos,D_training)

for n = 1:size(D_training,1) % number of rows
    
    if D_training(n,col_pos) ~= 0
        %compare row row_pos with the current n row
        % do the ratiings that are used in the sim need to be calculated by
        % both users?
        
        if 
            
        end
        
        % take a row --> find the other rows which are rated as much
    end
    
end

end




