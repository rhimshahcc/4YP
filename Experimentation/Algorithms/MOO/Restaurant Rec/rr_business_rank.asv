% A function to create a vendor ranking user row. 

function vendor_ranking = rr_business_rank(ratings_matrix,row_number)

user_row = ratings_matrix(row_number,:); % extract the row
 
row_length = size(user_row,2);

rand(1,row_length)

vendor_ranking =   % [ranking item]

end 
